# Veniar frontend environment variables
# Copy to .env.local for local development
# Set in Cloudflare Pages → Settings → Environment Variables for production

# Railway backend API base URL (no trailing slash)
REACT_APP_API_URL=https://rewardsnow-production.up.railway.app
